name: Fetch YouTube Analytics

on:
  schedule:
    # Once daily at 06:00 UTC. Was every 4 hours (6 runs/day); the per-video API call
    # loops (traffic-source + retention-window fetches, across 4 channels) were pushing
    # the YouTube Data/Analytics API daily quota to exhaustion. Daily cuts total call
    # volume ~6x. Adjust the hour freely — it's arbitrary, not tied to any other gotcha.
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - run: pip install -r requirements.txt

      - name: Run fetch
        env:
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_CHANNEL_ID_HW: ${{ secrets.YT_CHANNEL_ID_HW }}
          YT_REFRESH_TOKEN_HW: ${{ secrets.YT_REFRESH_TOKEN_HW }}
          YT_CHANNEL_ID_CGS: ${{ secrets.YT_CHANNEL_ID_CGS }}
          YT_REFRESH_TOKEN_CGS: ${{ secrets.YT_REFRESH_TOKEN_CGS }}
          YT_CHANNEL_ID_KZAK: ${{ secrets.YT_CHANNEL_ID_KZAK }}
          YT_REFRESH_TOKEN_KZAK: ${{ secrets.YT_REFRESH_TOKEN_KZAK }}
          YT_CHANNEL_ID_TCT: ${{ secrets.YT_CHANNEL_ID_TCT }}
          YT_REFRESH_TOKEN_TCT: ${{ secrets.YT_REFRESH_TOKEN_TCT }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: python fetch_metrics.py

      - name: Commit updated DB
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp data.db /tmp/data.db.new
          git fetch origin main
          git reset --hard origin/main
          cp /tmp/data.db.new data.db
          git add data.db
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "chore: update analytics snapshot"
            git push
          fi
